services:
  web:
    build: .
    ports:
      - "8000:8000"
      - "443:443"
    volumes:
      - ./backend:/app/backend
      - ./frontend:/app/frontend
    environment:
      - PYTHONDONTWRITEBYTECODE=1
